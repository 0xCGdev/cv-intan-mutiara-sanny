import { state, $, setToken, busy, toast } from './state.js';
import { api } from './api.js';
export function showLogin(){ $('loginView').classList.remove('hidden'); $('appView').classList.add('hidden'); setTimeout(()=>$('loginUser')?.focus(),50); }
export function enterApp(data){ $('loginView').classList.add('hidden'); $('appView').classList.remove('hidden'); $('userPill').textContent=`${state.me.name} · ${state.me.role}`; document.querySelectorAll('.admin-only').forEach(el=>el.classList.toggle('hidden',state.me.role!=='ADMIN')); }
export async function login(){ $('loginError').classList.add('hidden'); busy(true); try{ const r=await api('login',{username:$('loginUser').value,password:$('loginPass').value},false); if(!r.success){$('loginError').textContent=r.message||'Login gagal.';$('loginError').classList.remove('hidden');return;} setToken(r.token);state.me=r.user;const data=await api('getAppData');if(!data.success)throw new Error(data.message||'Gagal memuat aplikasi.');$('loginPass').value='';enterApp(data);window.dispatchEvent(new CustomEvent('app:ready',{detail:data})); }catch(e){console.error(e);$('loginError').textContent=e.message||'Tidak dapat terhubung ke server.';$('loginError').classList.remove('hidden');}finally{busy(false);} }
export async function logout(silent=false){ try{if(state.token)await api('logout');}catch(_){} setToken('');state.me=null;window.dispatchEvent(new Event('scanner:stop'));$('appView').classList.add('hidden');$('loginView').classList.remove('hidden');if(!silent)toast('Anda telah keluar.'); }
export async function startSession(){ if(!state.token){showLogin();return false;} busy(true);try{const r=await api('getAppData');if(!r.success)throw new Error(r.message);state.me=r.user;enterApp(r);window.dispatchEvent(new CustomEvent('app:ready',{detail:r}));return true;}catch(e){setToken('');showLogin();return false;}finally{busy(false);} }

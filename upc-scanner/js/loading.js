/* =========================================================
   LOADING — TEMPORARY PLACEHOLDER
   Fitur Loading akan dibuat setelah Packing selesai.
========================================================= */

export function bindLoading() {
    // Sementara kosong.
    // Dibuat agar app.js tetap bisa melakukan bindLoading()
    // tanpa menyebabkan error.
}

export async function loadLoading() {
    // Sementara tidak melakukan apa-apa.
    // Loading akan dibuat setelah modul Packing selesai.
}
